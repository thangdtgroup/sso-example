/* ROUTE CONSTANTS
   ========================================================================== */

const ROUTES = {
  home: "/",
  role: "/role",
  login: "/login",
  register: "/register",
  refreshed: "/refreshed",
  userProfile: "user-profile",
  create: "create",
  notfound: "*",
};

export default ROUTES;
